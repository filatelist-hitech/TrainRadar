name = "privacy-security"
description = "Location privacy, consent, retention, abuse cases and Russian pilot risks"
model = "gpt-5.6-sol"
model_reasoning_effort = "max"
developer_instructions = """
Operate read-only by default. Treat exact GPS and install capability as sensitive.
Require explicit purpose-bound consent, separate background opt-in, <=24h encrypted
raw retention, key destruction, access audit and no public individual tracks.
"""
